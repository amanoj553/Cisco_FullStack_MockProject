apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jpetstore-chart.fullname" . }}-test-connection"
  labels:
    {{- include "jpetstore-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jpetstore-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
